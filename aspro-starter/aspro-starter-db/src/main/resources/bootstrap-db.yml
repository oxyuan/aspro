---
spring:
  profiles: dev
  cloud:
    nacos:
      config:
        extension-configs:
          - data-id: aspro-starter-db.yaml
            group: aspro
            refresh: true

# prod profile
---
spring:
  profiles: prod
  cloud:
    nacos:
      config:
        extension-configs:
          - data-id: aspro-starter-db.yaml
            group: aspro
            refresh: true